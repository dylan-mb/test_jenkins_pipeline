pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'cat /client-config/rna/.env.rna.prod'
                echo 'Hello World'
            }
        }
    }
}
