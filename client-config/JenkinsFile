pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'cat client-config/agglo/.env.agglo.prod | grep TARGET_SERVER | cut -d= -f2-'
                echo 'Hello World'
            }
        }
    }
}
