pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'cat client-config/agglo/.env.agglo.prod'
                echo 'Hello World'
            }
        }
    }
}
